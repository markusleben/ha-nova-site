# ha-nova-site

Source for the HA NOVA landing page (GitHub Pages). The build brief lives in [`PROMPT.md`](PROMPT.md); brand assets are vendored in [`assets/`](assets/) from the released product state.

Content rule: this page follows the main repo's claim discipline — published features only, no version numbers, "App" (never "Add-on"), Cloud access always labeled Beta. Product truth lives at [markusleben/ha-nova](https://github.com/markusleben/ha-nova).
